/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    i18n: {
        locales: ["de_DE", "en_GB"],
        defaultLocale: "de_DE",
    },
};

module.exports = nextConfig;
